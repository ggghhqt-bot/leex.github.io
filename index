<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Para mi niña 💖</title>
     <link rel="stylesheet" href="styles.css">
    <style>
        * {
            box-sizing: border-box;
            font-family: 'Arial', cursive;
        }

        body {
            margin: 0;
            height: 100vh;
            background: linear-gradient(135deg, pink, #ffb6c1);
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* Corazones */
        .heart {
            position: absolute;
            color: red;
            font-size: 16px;
            animation: float 6s linear infinite;
            opacity: 0.6;
        }

        @keyframes float {
            0% { transform: translateY(100vh); opacity: 0; }
            50% { opacity: 0.7; }
            100% { transform: translateY(-10vh); opacity: 0; }
        }

        /* Tarjeta */
        .card {
            background: #ffd1e8;
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 25px rgba(0,0,0,0.25);
            z-index: 10;
            width: 340px;
            animation: pop 1s ease;
        }

        @keyframes pop {
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }

        h2, h3 {
            color: #fff;
        }

        p {
            font-size: 15px;
            color: #5a0033;
        }

        .buttons {
            margin-top: 25px;
            height: 70px;
            position: relative;
        }

        button {
            border: none;
            padding: 12px 22px;
            border-radius: 25px;
            cursor: pointer;
            font-size: 16px;
        }

        #yes {
            background: #ff4d88;
            color: white;
        }

        #no {
            background: #ff99bb;
            color: white;
            position: absolute;
            left: 140px;
        }

        /* Mensaje final */
        .final {
            display: none;
            font-size: 32px;
            color: #fff;
            font-weight: bold;
            animation: pop 1s ease;
        }

        /* Botón música */
        .music-btn {
            position: fixed;
            bottom: 15px;
            right: 15px;
            background: #ff4d88;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 20px;
            cursor: pointer;
            z-index: 20;
        }
    </style>
</head>

<body>

    <div class="card" id="card">
        <h2>Para mi Mayyonesa 💖</h2>
        <p>
            Desde que te conocí, cada día es más especial.  
            Admiro tu sonrisa, tu forma de ser y todo lo que eres ❤️
        </p>
        <h3>¿Quieres ser mi San Valentín?</h3>

        <div class="buttons">
            <button id="yes">Sí 💖</button>
            <button id="no">No 💔</button>
        </div>
    </div>

    <div class="final" id="finalText">
        💕 TE AMO MI GÜERITA 💕<br>
        FELIZ SAN VALENTÍN ❤️
    </div>

    <!-- Música -->
    <audio id="music" loop>
        <!-- CAMBIA EL ARCHIVO POR EL QUE QUIERAS -->
        <source src="corazon.mp3" type="audio/mpeg">
        
    </audio>

    <button class="music-btn" onclick="toggleMusic()">🎶 Música</button>

    <script>
        const noBtn = document.getElementById("no");

        function moveNo() {
            const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
            const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);

            noBtn.style.position = "fixed";
            noBtn.style.left = x + "px";
            noBtn.style.top = y + "px";
        }

        noBtn.addEventListener("mouseover", moveNo);
        noBtn.addEventListener("touchstart", moveNo);

        document.getElementById("yes").addEventListener("click", () => {
            document.getElementById("card").style.display = "none";
            document.getElementById("finalText").style.display = "block";
            document.getElementById("music").play();
        });

        function createHeart() {
            const heart = document.createElement("div");
            heart.classList.add("heart");
            heart.innerHTML = "❤️";
            heart.style.left = Math.random() * 100 + "vw";
            heart.style.fontSize = Math.random() * 20 + 10 + "px";
            document.body.appendChild(heart);
            setTimeout(() => heart.remove(), 6000);
        }

        setInterval(createHeart, 250);

        function toggleMusic() {
            const music = document.getElementById("music");
            music.paused ? music.play() : music.pause();
        }
    </script>

</body>
</html>
